# Changelog — Admin

All notable changes on `dev-alvin` relative to `dev`.

## [Unreleased] — 2026-07-26

### Added
- **Auth** — login, session guard, profile (general / security / sessions)
- **Users** — live pageable table (replaces mock data)
- **App content** — edit/publish wired to API
- **JLPT levels** — list, add/edit modal, lock/unlock confirms
- **Activation codes** — multi-level generate + pageable list with level chips
- **UX** — breadcrumbs, shared table pagination (10/20/50/Other)
- **Errors** — 404 / 500 / 503 pages, error boundary, `/status` health poll; network/503 → `/503`
